# Runtime dependencies for the Azure Functions app.
# NOTE: do NOT add any third-party Ecobee wrapper here — we talk to the
# official REST API directly via httpx.
azure-functions>=1.21.0
httpx>=0.27.0
azure-identity>=1.19.0
azure-keyvault-secrets>=4.9.0
