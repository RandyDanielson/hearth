[project]
name = "hearth"
version = "0.1.0"
description = "Comfort-optimizing automation for Ecobee thermostats (Azure Functions)."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 100
